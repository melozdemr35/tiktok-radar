name: Tam Otonom TikTok Casusu

on:
  schedule:
    # UTC saatine göre ayarlanır (TR saati için 3 saat çıkarıyoruz)
    - cron: '30 7 * * *'   # TR saatiyle 10:30
    - cron: '30 16 * * *'  # TR saatiyle 19:30
  workflow_dispatch:        # Bu da senin "Manuel" başlatmanı sağlar

permissions:
  contents: write # Robotun çektiği binlerce veriyi veritabanına kaydedebilmesi için ŞART!

jobs:
  otonom_islem:
    runs-on: ubuntu-latest
    # GitHub Actions varsayılan süresi 360 dakikadır, bizim 60 dakikalık işimiz için fazlasıyla yeterli.
    timeout-minutes: 90 
    
    steps:
      - name: Kodları Çek
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true
          fetch-depth: 0 

      - name: Python Kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Kütüphaneleri Hızlı Kur
        run: |
          pip install --no-cache-dir google-genai playwright pandas
          playwright install --with-deps chromium

      - name: TikTok'tan Video Çek ve Analiz Et
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TIKTOK_SESSION_1: ${{ secrets.TIKTOK_SESSION_1 }}
          TIKTOK_SESSION_2: ${{ secrets.TIKTOK_SESSION_2 }}
          TIKTOK_SESSION_3: ${{ secrets.TIKTOK_SESSION_3 }}
          TIKTOK_SESSION_4: ${{ secrets.TIKTOK_SESSION_4 }}
          TIKTOK_SESSION_5: ${{ secrets.TIKTOK_SESSION_5 }}
        run: |
          # Bu komut otonom_motor.py içindeki 60 dakikalık döngüyü başlatır
          python otonom_motor.py

      - name: Değişiklikleri GitHub'a Kaydet
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          
          git add trend_veritabani.json son_analiz.txt
          
          git commit -m "Otonom Veri Güncellemesi [$(date)]" || echo "Degisiklik yok"
          
          # Veritabanı şişerken çakışma olmaması için 'theirs' stratejisiyle pull yap
          git pull origin main --strategy-option=theirs --no-rebase || echo "Pull atlandı"
          
          git push origin main --force
